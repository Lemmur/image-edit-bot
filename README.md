# Qwen Image Edit Bot 🎨

Telegram бот для редактирования изображений с использованием **Qwen Image Edit** модели через ComfyUI.

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![aiogram](https://img.shields.io/badge/aiogram-3.x-blue.svg)](https://docs.aiogram.dev/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

## 🌟 Возможности

- ✅ Редактирование изображений по текстовым промптам
- ✅ Поддержка photo и document (оригинальное качество)
- ✅ Полный контроль параметров (steps, CFG, seed, sampler)
- ✅ Inline клавиатуры для настроек
- ✅ FIFO очередь задач с отслеживанием прогресса
- ✅ WebSocket real-time обновления
- ✅ Автоматическая очистка файлов
- ✅ Graceful shutdown для production
- ✅ Готовые systemd сервисы
- ✅ Автоматическое обновление из git

## 📋 Требования

### Сервер
- Ubuntu 22.04+ или Debian 12+ / macOS / Windows
- NVIDIA GPU (RTX 3090 24GB рекомендуется) или Apple Silicon
- NVIDIA Driver 535+ (для CUDA)
- CUDA 12.4+ (для NVIDIA)
- Python 3.10+
- ~50 GB свободного места

### ComfyUI
Бот может работать с уже установленной версией ComfyUI или установить её автоматически.

### Зависимости
Все зависимости указаны в `requirements.txt`:
- aiogram 3.15.0 — Telegram bot framework
- aiohttp 3.10.11 — Async HTTP client
- websockets 13.1 — WebSocket client
- Pillow 10.4.0 — Image processing
- python-dotenv 1.0.0 — Environment variables
- PyYAML 6.0.2 — YAML config
- pydantic 2.9.2 — Data validation
- loguru 0.7.3 — Logging

## ⚠️ Устранение неполадок

Если возникли проблемы при установке (например, ошибка `cho: command not found`), см. **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)**

## 🚀 Быстрый старт — ОДНА КОМАНДА!

### Автоматическая установка на сервер

```bash
# Скачать и запустить установку (требует root)
curl -fsSL https://raw.githubusercontent.com/Lemmur/image-edit-bot/main/scripts/quick_deploy.sh | sudo bash
```

**Или клонировать репозиторий и запустить:**

```bash
git clone https://github.com/Lemmur/image-edit-bot.git
cd image-edit-bot
sudo bash scripts/quick_deploy.sh
```

Скрипт автоматически установит:
- ✅ ComfyUI + PyTorch + CUDA
- ✅ Модели Qwen Image Edit (~25GB)
- ✅ Кастомные ноды ComfyUI
- ✅ Telegram бота
- ✅ Systemd сервисы

### Настройка токена бота

```bash
# Отредактировать .env файл
sudo nano /opt/image-edit-bot/.env
```

Добавьте токен бота (см. секцию [Создание Telegram бота](#-создание-telegram-бота)):
```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
ADMIN_USER_IDS=123456789,987654321  # Опционально: whitelist пользователей
```

### Запуск

```bash
# Запустить ComfyUI
sudo systemctl start comfyui

# Подождать ~60 сек (загрузка моделей)
# Проверить доступность:
curl http://127.0.0.1:8188/system_stats

# Запустить бота
sudo systemctl start telegram-bot

# Проверить логи
sudo journalctl -u telegram-bot -f
```

## 🔧 Использование с установленным ComfyUI

Если у вас уже установлен ComfyUI, бот может использовать его вместо автоматической установки.

### Настройка

1. Укажите путь к ComfyUI в `.env`:

```bash
# Путь к установленному ComfyUI (обязательно для авто-запуска)
COMFYUI_DIR=/path/to/your/ComfyUI

# Путь к виртуальному окружению (опционально, по умолчанию COMFYUI_DIR/venv)
COMFYUI_VENV=/path/to/venv

# Автоматически запускать ComfyUI если не запущен (true/false)
COMFYUI_AUTO_START=true

# Дополнительные аргументы запуска
COMFYUI_ARGS=--cuda-device 0
```

2. Запустите бота:

```bash
python -m src.main
```

Бот автоматически:
- Проверит, запущен ли ComfyUI
- Если нет — запустит его из указанной директории
- Дождётся готовности и начнёт работу

### Управление ComfyUI вручную

```bash
# Проверить статус
python scripts/manage_comfyui.py status

# Запустить ComfyUI
python scripts/manage_comfyui.py start

# Остановить (инструкции)
python scripts/manage_comfyui.py stop
```

### Режим без авто-запуска

Если ComfyUI запускается отдельно (например, через systemd):

```bash
COMFYUI_AUTO_START=false
COMFYUI_HOST=127.0.0.1
COMFYUI_PORT=8188
```

## 🔄 Обновление бота

Обновить бот из git и перезапустить **одной командой**:

```bash
sudo bash /opt/image-edit-bot/scripts/update_bot.sh
```

Скрипт автоматически:
- 📥 Загружает последние обновления из git
- 💾 Сохраняет локальные изменения
- 📦 Обновляет Python зависимости
- 🔄 Перезапускает бота
- ✅ Проверяет статус запуска

### Ручное обновление (альтернативный способ)

```bash
cd /opt/image-edit-bot
sudo -u comfyui git pull
sudo -u comfyui bash -c 'source venv/bin/activate && pip install -r requirements.txt'
sudo systemctl restart telegram-bot
```

## 🤖 Создание Telegram бота

Перед установкой на сервер нужно создать бота в Telegram и получить токен.

### Шаг 1: Открыть BotFather

1. Откройте Telegram (мобильное приложение или веб-версию)
2. Найдите [@BotFather](https://t.me/botfather) — официального бота для управления ботами
3. Запустите диалог командой `/start`

### Шаг 2: Создать нового бота

Отправьте команду:
```
/newbot
```

BotFather попросит указать:

**1. Имя бота (display name):**
```
Qwen Image Editor
```
Это имя будет отображаться в списках чатов

**2. Username бота (должен заканчиваться на `bot`):**
```
qwen_image_edit_bot
```
⚠️ Username должен быть уникальным. Если занят — попробуйте вариации:
- `my_qwen_edit_bot`
- `qwen_img_editor_bot`
- `yourname_qwen_bot`

### Шаг 3: Получить токен

После успешного создания BotFather отправит сообщение:

```
Done! Congratulations on your new bot. You will find it at t.me/qwen_image_edit_bot.
You can now add a description...

Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890

Keep your token secure and store it safely, it can be used by anyone to control your bot.
```

**Скопируйте токен!** Формат: `<число>:<случайная строка>`

**⚠️ ВАЖНО:**
- Никому не показывайте токен
- Не публикуйте его в открытом доступе
- Если токен утек — используйте `/revoke` в BotFather для генерации нового

### Шаг 4: Настроить команды бота (опционально)

Для удобного меню команд отправьте BotFather:

```
/setcommands
```

Выберите вашего бота, затем отправьте список команд:

```
start - Начать работу с ботом
help - Показать справку и примеры
new - Создать новую задачу редактирования
status - Показать статус очереди
cancel - Отменить текущую задачу
```

### Шаг 5: Настроить описание бота (опционально)

**Короткое описание** (отображается в списке чатов):
```
/setdescription
```
Текст:
```
Бот для редактирования изображений с помощью AI модели Qwen Image Edit. Отправьте фото и промпт — получите результат!
```

**About секция** (в информации о боте):
```
/setabouttext
```
Текст:
```
🎨 Qwen Image Edit Bot

Редактирование изображений через AI модель Qwen2-VL.

Возможности:
• Удаление фона
• Изменение стиля
• Добавление объектов
• Улучшение качества

Powered by ComfyUI + Stable Diffusion
```

### Шаг 6: Проверить бота

1. Найдите бота в Telegram по username: `@qwen_image_edit_bot`
2. Отправьте `/start`
3. Пока сервер не настроен — бот не ответит (это нормально)

### 🔒 Приватность и whitelist

Если хотите ограничить доступ к боту только для себя:

1. Узнайте свой Telegram User ID:
   - Напишите [@userinfobot](https://t.me/userinfobot)
   - Он пришлет ваш ID (например: `123456789`)

2. Добавьте в `.env`:
```bash
ADMIN_USER_IDS=123456789,987654321
```

Теперь бот будет отвечать только пользователям из этого списка.

---

## 📖 Использование

### Простой режим

1. Отправьте `/new` боту
2. Отправьте изображение (photo или document)
3. Напишите промпт: `"remove background, make professional portrait"`
4. (Опционально) Negative prompt или `/skip`
5. Нажмите ✅ **Запустить**
6. Получите результат!

### Продвинутый режим

1. После `/new` и загрузки изображения
2. Нажмите ⚙️ **Настройки**
3. Настройте параметры:
   - **Steps** (1-50): Количество шагов генерации
   - **CFG** (0.1-20.0): Classifier-Free Guidance
   - **Seed**: Random или фиксированный
   - **Sampler**: Метод сэмплинга (euler, heun, dpm, lms)
4. Нажмите ✅ **Применить** → ✅ **Запустить**

### Команды

```
/start - Приветствие и список команд
/help - Помощь и примеры использования
/new - Начать новую задачу редактирования
/status - Статус очереди (текущая задача, размер очереди)
/cancel - Отменить текущую задачу
/skip - Пропустить negative prompt
```

## 🏗️ Архитектура

```
┌─────────────────┐
│  Telegram User  │
└────────┬────────┘
         │
    ┌────▼─────┐
    │ aiogram  │ Bot + FSM + Handlers
    │   Bot    │
    └────┬─────┘
         │
    ┌────▼──────┐
    │Task Queue │ asyncio.Queue (FIFO)
    └────┬──────┘
         │
    ┌────▼──────────┐
    │Task Processor │ Background worker
    └────┬──────────┘
         │
    ┌────▼────────┐
    │ ComfyUI API │ REST + WebSocket
    └─────────────┘
```

Подробная архитектура: [`plans/ARCHITECTURE.md`](plans/ARCHITECTURE.md)

## 📁 Структура проекта

```
image-edit-bot/
├── src/
│   ├── main.py                 # Точка входа
│   ├── bot/                    # Telegram bot
│   │   ├── handlers.py         # Обработчики команд
│   │   ├── keyboards.py        # Inline клавиатуры
│   │   ├── states.py           # FSM состояния
│   │   └── filters.py          # Фильтры (whitelist, rate limit)
│   ├── comfyui/                # ComfyUI интеграция
│   │   ├── client.py           # REST API клиент
│   │   ├── websocket.py        # WebSocket прогресс
│   │   └── workflow.py         # Workflow manager
│   ├── queue/                  # Система очередей
│   │   ├── task_queue.py       # FIFO очередь
│   │   └── processor.py        # Обработчик задач
│   ├── models/                 # Модели данных
│   │   ├── config.py           # Pydantic конфигурация
│   │   └── task.py             # Task dataclass
│   ├── storage/                # Файловое хранилище
│   │   └── file_manager.py     # Управление файлов
│   └── utils/                  # Утилиты
│       ├── config_loader.py    # Загрузка конфигурации
│       └── logger.py           # Настройка логирования
├── scripts/                    # Скрипты деплоя
│   ├── quick_deploy.sh         # 🚀 БЫСТРАЯ УСТАНОВКА (одна команда!)
│   ├── update_bot.sh           # 🔄 Обновление из git
│   ├── setup_server.sh         # Полная установка (устарело, используйте quick_deploy.sh)
│   ├── install_comfyui.sh      # Установка ComfyUI
│   ├── download_models.sh      # Скачивание моделей
│   └── setup_services.sh       # Systemd сервисы
├── workflows/                  # ComfyUI workflows
│   └── qwen_image_edit.json    # Базовый workflow (API формат)
├── tests/                      # Unit тесты
├── config.yaml                 # Конфигурация параметров
├── .env.example                # Пример переменных окружения
└── requirements.txt            # Python зависимости
```

## ⚙️ Конфигурация

### `config.yaml`

Основная конфигурация параметров workflow:

```yaml
workflow:
  defaults:
    steps: 8
    cfg: 1.0
    sampler: "linear/euler"
    negative_prompt: "ugly, blurry, distorted..."
  limits:
    min_steps: 1
    max_steps: 50

queue:
  max_size: 100
  timeout_seconds: 300

storage:
  cleanup_after_hours: 24
  keep_results: true
```

### `.env`

Секретные данные:

```bash
TELEGRAM_BOT_TOKEN=your_token_here
ADMIN_USER_IDS=123,456,789  # Опционально
COMFYUI_HOST=127.0.0.1
COMFYUI_PORT=8188
```

## 🔧 Управление сервисами

```bash
# Запуск
sudo systemctl start comfyui
sudo systemctl start telegram-bot

# Остановка
sudo systemctl stop telegram-bot
sudo systemctl stop comfyui

# Автозапуск
sudo systemctl enable comfyui
sudo systemctl enable telegram-bot

# Логи
sudo journalctl -u comfyui -f
sudo journalctl -u telegram-bot -f

# Статус
sudo systemctl status telegram-bot
sudo systemctl status comfyui
```

## 🔄 Обновление и обслуживание

### Автоматическое обновление (рекомендуется)

```bash
# Обновить бот из git и перезапустить
sudo bash /opt/image-edit-bot/scripts/update_bot.sh
```

### Ручное обновление

```bash
cd /opt/image-edit-bot
sudo -u comfyui git pull
sudo -u comfyui bash -c 'source venv/bin/activate && pip install -r requirements.txt'
sudo systemctl restart telegram-bot
```

### Откат к предыдущей версии

```bash
cd /opt/image-edit-bot
sudo -u comfyui git log --oneline -10  # Посмотреть список коммитов
sudo -u comfyui git reset --hard COMMIT_HASH
sudo systemctl restart telegram-bot
```

### Восстановление из stash

```bash
cd /opt/image-edit-bot
sudo -u comfyui git stash list  # Посмотреть сохраненные изменения
sudo -u comfyui git stash pop   # Восстановить последние изменения
```

## 🧪 Тестирование

```bash
# Проверка подключения к ComfyUI
python scripts/test_comfyui.py

# Unit тесты
pytest tests/ -v

# Тест бота локально (без systemd)
python -m src.main
```

## 🐛 Отладка

### ComfyUI не запускается

```bash
# Проверить логи
sudo journalctl -u comfyui -n 100

# Проверить модели
ls -lh /opt/ComfyUI/models/checkpoints/
ls -lh /opt/ComfyUI/models/text_encoders/

# Ручной запуск для диагностики
sudo -u comfyui /opt/ComfyUI/venv/bin/python /opt/ComfyUI/main.py
```

### Бот не отвечает

```bash
# Проверить токен
sudo cat /opt/image-edit-bot/.env | grep TOKEN

# Проверить подключение к ComfyUI
curl http://127.0.0.1:8188/system_stats

# Проверить логи бота
sudo journalctl -u telegram-bot -n 100 --no-pager
```

### Очередь зависла

```bash
# Проверить статус через бота
# Отправить команду /status

# Перезапустить бота (сбросит очередь)
sudo systemctl restart telegram-bot
```

### Ошибки с изображениями

```bash
# Проверить права на директории
ls -la /opt/image-edit-bot/data/temp/
ls -la /opt/image-edit-bot/data/results/

# Исправить права
sudo chown -R comfyui:comfyui /opt/image-edit-bot/data/
```

### Обновление не работает

```bash
# Проверить git репозиторий
cd /opt/image-edit-bot
sudo -u comfyui git status
sudo -u comfyui git remote -v

# Переинициализировать git
sudo -u comfyui git remote set-url origin https://github.com/Lemmur/image-edit-bot.git
sudo -u comfyui git fetch origin
```

## 📊 Мониторинг

Метрики доступны через:
- **Bot logs**: `/opt/image-edit-bot/logs/bot.log`
- **Error logs**: `/opt/image-edit-bot/logs/errors.log`
- **Queue status**: `/status` команда в боте
- **Storage stats**: Встроенная команда FileManager

## 🔐 Безопасность

- ✅ ComfyUI доступен только локально (127.0.0.1)
- ✅ Опциональный whitelist пользователей
- ✅ Rate limiting защита от спама
- ✅ Валидация входных данных
- ✅ Автоочистка временных файлов
- ✅ Изоляция процессов через systemd
- ✅ Автоматическое сохранение локальных изменений при обновлении

## 📈 Масштабирование (опционально)

Для большой нагрузки можно добавить:
- Redis Queue вместо asyncio.Queue
- Multiple workers для параллельной обработки
- Load balancer для нескольких ComfyUI инстансов
- S3/MinIO для хранения результатов
- PostgreSQL для истории задач
- Prometheus + Grafana для мониторинга

## 🤝 Contributing

Pull requests приветствуются! Для больших изменений создайте issue для обсуждения.

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)

## 🙏 Благодарности

- [ComfyUI](https://github.com/comfyanonymous/ComfyUI) - UI для Stable Diffusion
- [Qwen Image Edit](https://huggingface.co/Phr00t/Qwen-Image-Edit-Rapid-AIO) - Модель редактирования
- [aiogram](https://docs.aiogram.dev/) - Telegram Bot framework
- [RES4LYF](https://github.com/ClownsharkBatwing/RES4LYF) - ClownsharKSampler
